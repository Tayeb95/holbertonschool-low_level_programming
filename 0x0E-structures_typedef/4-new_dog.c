#include <stdio.h>
#include "dog.h"
/**
 * *new_dog - does nothing
 * @name: doge name
 * @age: doge age
 * @owner: doge owner
 * Return: anime doge
 */
dog_t *new_dog(char *name, float age, char *owner)
{
	dog_t *korone;

	korone = malloc(sizeof(dog_t));
	if (dog == NULL)
		return (NULL);
	return (korone);
}
