#include <stdio.h>
/**
 * main - main block
 * Description: Print all numbers of base 10, starting from 0.
 * Return: 0
 */
int main(void)
{
	int t;

	for (t = 0; t < 10; i++)
	{
		printf("%i", i);
	}
	putchar('\n');

	return (0);
}
